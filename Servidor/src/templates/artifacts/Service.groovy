@artifact.package@import org.apache.log4j.Logger
class @artifact.name@ {

    private static final log = Logger.getLogger(@artifact.name@.class)
    boolean transactional = true

    def serviceMethod() {

    }
}
